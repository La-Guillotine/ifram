{% extends 'base.html.twig' %}

{% block title %}Edit Plante{% endblock %}

{% block body %}
    <h1>Edit Plante</h1>

    {{ include('plante/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <a class="btn btn-primary" href="{{ path('plante_index') }}">Retour à la liste des plantes</a>

    {{ include('plante/_delete_form.html.twig') }}
{% endblock %}
